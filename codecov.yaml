coverage:
  status:
    project:
      default:
        target: 60%       # Set your desired threshold here
        threshold: 0%     # Allowed drop before failing
        if_no_uploads: error
        informational: false
    patch:
      default:
        target: 60%
        threshold: 0%
        informational: false
