coverage:
  status:
    project:
      default:
        target: 80%       # Set your desired threshold here
        threshold: 2%     # Allowed drop before failing
        if_no_uploads: error
    patch:
      default:
        target: 80%
        threshold: 1%
