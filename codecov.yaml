coverage:
  status:
    project:
      default:
        target: 40%       # Set your desired threshold here
        threshold: 0%     # Allowed drop before failing
        if_no_uploads: error
    patch:
      default:
        target: 40%
        threshold: 0%
