coverage:
  status:
    project:
      default:
        target: 57.5%       # Set your desired threshold here
        threshold: 0%     # Allowed drop before failing
        if_no_uploads: error
        informational: false
    patch:
      default:
        target: 57.5%
        threshold: 0%
        informational: false
  ignore:
    - "**/*.md"
    - "**/README.md"
    - "**/docs/**"
    - "**/CHANGELOG.md"
